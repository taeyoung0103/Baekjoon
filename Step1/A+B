import java.util.Scanner;
//사용자로부터 키보드 입력, 혹은 어떠한 값을 입력받기위해 Scanner를 사용해야하는데, 이를 사용하기 위해서 import를 해야함

public class Main {
    public static void main(String[] args) {
//public static void - 여러개의 접근제어자들 중 public은 '접근제한이 없고, 자바 프로젝트 내에서 자유롭게 접근 가능함' 을 의미
//static은 클래스에 접근할때 인스턴스가 필요한데, 이 인스턴스 생성 없이 접근가능하게 도와줌. 
//예) Example e = new Example(); - Example의 새 인스턴스를 생성하고, 참조변수 e가 해당 인스턴스를 가리키게함.  e.pub(); - 참조변수 e를 통해 Example 클래스의 pub() 메서드에 접근 
//void - return값이 필요하지 않을때 사용 - 여기서 main은 따로 return값을 필요로 하지 않는다.
//String[] args - 매개변수이며, 메서드가 기능을 수행하기 위해 필요한 값(인자, argument)들을 받아오는 변수이다. args라는 이름을 가진 String 배열 타입의 매개변수를 가지며, string형태의 문자열을 가져온다.
        Scanner sc = new Scanner(System.in);
//Scanner 객체를 생성하며, System.in = 키보드 등을 통해 입력받은 값 - 을 통해 얻은 값
        int a = sc.nextInt();
//얻은 정수값을 a에 저장
        int b = sc.nextInt();
//얻은 정수값을 b에 저장
        System.out.println(a+b);
//얻어서 저장한 a값과 b값을 더하고, 출력
    }
}
